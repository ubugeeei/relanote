{
  "name": "relanote-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "relanote — a pure functional, statically-typed language for music. Workspace root.",
  "scripts": {
    "build": "moon build",
    "check": "moon check",
    "test": "moon test",
    "test:native": "moon test --target native -p ubugeeei/relanote/relanote_lsp -p ubugeeei/relanote/cmd/relanote",
    "ci": "pnpm run check && pnpm run line-limit && pnpm run test && pnpm run test:native && pnpm run build && pnpm run lsp:test && pnpm run coverage && pnpm run site:build",
    "line-limit": "fail=0; for f in $(git ls-files '*.mbt' '*.mbtv' '*.sh' '*.json' '*.yml' '*.yaml' '*.js' '*.mjs' '*.css' '*.html'); do [ -f \"$f\" ] || continue; n=$(wc -l < \"$f\"); if [ \"$n\" -gt 250 ]; then echo \"$n $f\"; fail=1; fi; done; exit $fail",
    "fmt": "moon fmt",
    "cli": "moon run cmd/relanote --",
    "run": "moon run cmd/relanote --",
    "lsp": "moon run cmd/relanote -- lsp",
    "lsp:test": "bash scripts/test_lsp_stdio.sh",
    "coverage": "moon coverage analyze -- -f summary",
    "coverage:html": "moon coverage analyze -- -f html -o coverage.html",
    "stdlib:regen": "bash scripts/gen_stdlib.sh",
    "site:build": "node scripts/build_site.mjs",
    "web:build": "moon run .mooncakes/ubugeeei/vapor_moon/src/cmd/vapor_moon -- compile web/App.mbtv",
    "tasks": "printf 'build\\ncheck\\ntest\\ntest:native\\nci\\nfmt\\ncli\\nrun\\nlsp\\nlsp:test\\ncoverage\\ncoverage:html\\nstdlib:regen\\nsite:build\\nweb:build\\n'"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  }
}
