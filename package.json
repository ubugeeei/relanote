{
  "name": "relanote-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "relanote — a pure functional, statically-typed language for music. Workspace root.",
  "scripts": {
    "build": "cd moonbit && moon build",
    "check": "cd moonbit && moon check",
    "test": "cd moonbit && moon test",
    "ci": "pnpm run check && pnpm run test",
    "fmt": "cd moonbit && moon fmt",
    "cli": "cd moonbit && moon run cmd/relanote --",
    "run": "cd moonbit && moon run cmd/relanote --",
    "lsp": "cd moonbit && moon run cmd/relanote -- lsp",
    "stdlib:regen": "bash moonbit/scripts/gen_stdlib.sh",
    "web:build": "cd moonbit && moon run .mooncakes/ubugeeei/vapor_moon/src/cmd/vapor_moon -- compile web/App.mbtv",
    "tasks": "printf 'build\\ncheck\\ntest\\nci\\nfmt\\ncli\\nrun\\nlsp\\nstdlib:regen\\nweb:build\\n'"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  }
}
